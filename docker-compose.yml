networks:
  aispeg-net:
    driver: bridge
    ipam:
      config:
        - subnet: 10.10.9.0/24

services:
  aispeg-prod:
    build: .
    container_name: aispeg-prod
    restart: unless-stopped
    environment:
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
    ports:
      - "9260:3000"
    networks:
      - aispeg-net
    profiles:
      - prod

  aispeg-dev:
    build: .
    container_name: aispeg-dev
    restart: unless-stopped
    environment:
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
    ports:
      - "9270:3000"
    networks:
      - aispeg-net
    profiles:
      - dev
